const show = () => {
    document.querySelector('.menu-mobile').
    style.transform = 'translateX(0)'
};

const hide = () => {
    document.querySelector('.menu-mobile').
    style.transform = 'translateX(100%)'
};